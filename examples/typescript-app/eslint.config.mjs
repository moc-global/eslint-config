import { moc } from '@moc-global/eslint-config';

// Zero-config: the stack (Node/TypeScript) and add-ons are auto-detected from
// this project's dependencies. Path aliases are read from tsconfig.json.
export default moc();
